# LifeFlow Blood Bank — Python Dependencies
# Install with:  pip install -r requirements.txt

flask>=2.3.0
flask-mail>=0.9.1
mysql-connector-python>=8.0.0

# Feature 1 — ML Demand Prediction
scikit-learn>=1.3.0
numpy>=1.24.0

# Feature 3 — PDF Report Generation
reportlab>=4.0.0

# Optional Python packages:
flask-socketio>=5.3.0
#   openpyxl>=3.1.0          - Excel export feature
#   google-generativeai>=0.8 - Gemini AI chatbot (pip install google-generativeai)

# Environment Variables Required:
#   MAIL_USERNAME       - Gmail address for sending alerts
#   MAIL_PASSWORD       - Gmail App Password
#   GEMINI_API_KEY      - Google Gemini API key for AI chatbot (get at aistudio.google.com)
#                         Set in app.py line ~73: GEMINI_API_KEY = "your-key-here"
#                         Without this key, chatbot falls back to keyword-based FAQ mode
